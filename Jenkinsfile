pipeline {
    agent any
    tools {
        maven 'maven3'
    }
    
    stages{
        stage('Build'){
            steps{
                // mvn clean package
                 //sh script: 'mvn clean package'
                 //archiveArtifacts artifacts: 'target/*.war', onlyIfSuccessful: true
                
            }
        }

    }
}
